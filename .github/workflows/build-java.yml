name: Build Java Yolks

on:
  push:
    branches:
      - main
    paths:
      - "java/**"
      - ".github/workflows/build-java.yml"

  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    name: Build Java ${{ matrix.version }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        version:
          - "8"
          - "11"
          - "16"
          - "17"
          - "18"
          - "19"
          - "20"
          - "21"
          - "22"

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./java/${{ matrix.version }}
          push: true
          tags: |
            ghcr.io/acore-hosting/java:${{ matrix.version }}
            ghcr.io/acore-hosting/java:${{ matrix.version }}-${{ github.sha }}
